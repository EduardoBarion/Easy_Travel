<div class="mt-4">
  <h1> Trips </h1>

  <ul>
    <% @trips.each do |trip| %>
      <li><%= link_to trip.name, trip_path(trip) %></li>
    <% end %>
  </ul>

  <%= link_to "Home Page", root_path, class: "btn btn-outline-secondary" %>

</div>
